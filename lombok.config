# 表示这是项目根目录，不再向上查找父目录的配置文件。
config.stopBubbling = true
# 表示生成的 toString 方法需要调用父类的 toString 方法。
lombok.tostring.callsuper=CALL
# 表示生成的 equals 和 hashCode 方法需要调用父类的 equals 和 hashCode 方法。
lombok.equalsandhashcode.callsuper=CALL
# 表示生成的 setter 方法返回 this，支持链式调用。
lombok.accessors.chain=true
